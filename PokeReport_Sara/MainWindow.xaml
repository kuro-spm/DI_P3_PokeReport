<Window x:Class="PokeReport_Sara.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:PokeReport_Sara"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        WindowState="Maximized"
        Icon="/Media/Pokeicon2.ico"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <wpf:WebView2 x:Name="BrowserView" Visibility="Hidden"/>

        <Grid x:Name="LoadingScreen" Background="White">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="TxtStatus" 
                       Text="Iniciant Pokédex..." 
                       FontSize="24" 
                       FontWeight="Bold"
                       Foreground="#333333"
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>

                <ProgressBar x:Name="BarraProgreso" 
                         Width="300" 
                         Height="30" 
                         Minimum="0" 
                         Maximum="100"
                         Foreground="#0075BE"/>
                <TextBlock Text="Por favor, espera..." 
                       FontSize="12" 
                       Foreground="Gray"
                       HorizontalAlignment="Center" 
                       Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
